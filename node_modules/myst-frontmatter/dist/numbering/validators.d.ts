import type { ValidationOptions } from 'simple-validators';
import type { Numbering, NumberingItem } from './types.js';
export declare const NUMBERING_OPTIONS: string[];
export declare const NUMBERING_KEYS: string[];
export declare const NUMBERING_ALIAS: {
    sections: string;
    h1: string;
    h2: string;
    h3: string;
    h4: string;
    h5: string;
    h6: string;
    heading1: string;
    heading2: string;
    heading3: string;
    heading4: string;
    heading5: string;
    heading6: string;
};
/**
 * Validate value for each numbering entry
 *
 * Value may be:
 * - boolean, to simply enable/disable numbering
 * - number, to indicate the starting number
 * - string, to define the cross-reference template
 *   (e.g. 'Fig. %s' to get "Fig. 1" instead of "Figure 1" in your document)
 * - An object with any of enabled/start/template - specifying the above types
 *   will coerce to this object
 */
export declare function validateNumberingItem(input: any, opts: ValidationOptions): NumberingItem | undefined;
/**
 * Validate Numbering object
 */
export declare function validateNumbering(input: any, opts: ValidationOptions): Numbering | undefined;
export declare function fillNumbering(base?: Numbering, filler?: Numbering): Numbering;
//# sourceMappingURL=validators.d.ts.map