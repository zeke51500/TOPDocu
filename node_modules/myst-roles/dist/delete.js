export const deleteRole = {
    name: 'delete',
    alias: ['del', 'strike'],
    body: {
        type: 'myst',
        required: true,
    },
    run(data) {
        return [{ type: 'delete', children: data.body }];
    },
};
