export const chemRole = {
    name: 'chemicalFormula',
    alias: ['chem'],
    body: {
        type: String,
        required: true,
    },
    run(data) {
        return [{ type: 'chemicalFormula', value: data.body }];
    },
};
