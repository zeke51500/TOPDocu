export const underlineRole = {
    name: 'underline',
    alias: ['u'],
    body: {
        type: 'myst',
        required: true,
    },
    run(data) {
        return [{ type: 'underline', children: data.body }];
    },
};
